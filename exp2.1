#include <iostream>
using namespace std;

int main() {
    int N, n;
    cin >> N >> n;

    if ((N >> n) & 1)
        cout << "Bit is SET";
    else
        cout << "Bit is NOT SET";

    return 0;
}
