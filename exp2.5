#include <iostream>
#include<vector>
using namespace std;

int main() {
    long long P;
    cin >> P;

    vector<int> digits;

    while (P > 0) {
        digits.push_back(P % 10);
        P /= 10;
    }

    cout << digits.size();
    return 0;
}
