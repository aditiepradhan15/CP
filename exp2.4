#include <iostream>
using namespace std;

int main() {
    int P;
    cin >> P;

    if (P == 0 || P == 1) {
        cout << 1;
        return 0;
    }

    double digits = 0;
    for (int i = 2; i <= P; i++) {
        digits += log10(i);
    }

    cout << floor(digits) + 1;
    return 0;
}
